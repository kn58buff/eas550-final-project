# Copy to .env (gitignored) for local development. Fill in real values.
# On Render, set these in the service's Environment tab — never commit them.
PGHOST=your-host.neon.tech
PGPORT=5432
PGDATABASE=supply_chain
PGUSER=your_user
PGPASSWORD=your_password
PGSSLMODE=require
PG_POOL_MAX=5
